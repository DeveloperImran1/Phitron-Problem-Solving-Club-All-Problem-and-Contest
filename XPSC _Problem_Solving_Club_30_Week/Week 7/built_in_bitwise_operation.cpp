#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << __builtin_popcount(39) << endl; // Output: 4 --> 39 er binary value te total 4 ta 1 ase. I mean 4 ta bin On ase.
    cout << __lg(39) << endl;               // Output: 4 --> 39 er binary value te highest set bit kon position a ase. I mean sobar last a kon bit on ase. Sei bit er position.

    return 0;
}